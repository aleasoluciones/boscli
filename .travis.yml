language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
install: "pip install -r requirements.txt -r requirements-dev.txt --use-mirrors"
before_script: python setup.py develop
script: mamba --enable-coverage
after_success: 'coverage report --include="boscli/*","spec/*"'
